//Collect variable from user
let countDown = parseInt(prompt('Please enter a number between 1 and 100 to begin countdown.'))

//Evaluate the user input and log count
for (i = countDown; i >= 0; i--) {
    console.log(i)
}